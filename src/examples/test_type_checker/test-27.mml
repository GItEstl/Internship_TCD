// Syntax error: conditional expression with only an expression inside one branch

func void test27 (string s) {
def 
    int x;
    int y;
    boolean b
in
    b = true;
    y = 10;
    x = if (b) {y} else {b = false}
}

start test27("")