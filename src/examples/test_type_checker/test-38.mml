// Without error: retrieval of the first and second elenent of a tuple and assignment of a binary expression with these two telements to an integer variable

func void test38 (string s) {
def
    (int, int) tuple;
    int i;
in
    tuple = (2,10);
    i = snd(tuple) + fst(tuple)
}

start test38("") 